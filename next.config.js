/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "res.cloudinary.com",
      "source.unsplash.com",
      "twitter.com",
    ],
  },
};

module.exports = nextConfig;
